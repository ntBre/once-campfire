Place the Unicode emoji-test.txt file in this directory.

You can download it from Unicode and then run:

  python3 script/generate_emoji_json.py

This will generate app/assets/emoji/emoji_list.json for the client-side emoji autocomplete.
